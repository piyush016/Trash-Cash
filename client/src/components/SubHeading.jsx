export default function SubHeading({ label }) {
  return (
    <div
      style={{
        color: "#718096",
        fontSize: "1rem",
        padding: "0.25rem 0 1rem",
      }}
    >
      {label}
    </div>
  );
}
